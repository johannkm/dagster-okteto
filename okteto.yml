name: dagit
labels:
  component: dagit
image: ramiro/dagster:dev
build:
  dockerfile: .dev.Dockerfile
command:
- bash
workdir: /usr/src/app
forward:
- 8080:80
persistentVolume:
  enabled: true
  size: 20Gi
resources:
  limits: 
    memory: 2Gi
    cpu: 1
    
