name: dagit
labels:
  component: dagit
image: ramiro/dagster:dev
build:
  dockerfile: .dev.Dockerfile
command:
- bash
workdir: /usr/src/app
forward:
- 8080:80
persistentVolume:
  enabled: true
resources:
  limits: 
    memory: 1Gi
    cpu: 1
